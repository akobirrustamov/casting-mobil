import { View } from 'react-native';

import { Logo } from '@/components/ui/Logo';

/**
 * S01 — экран загрузки: знак UzCasting на чёрном, и больше ничего.
 *
 * Лежит поверх навигатора, а не отдельным маршрутом: иначе «/» конфликтует
 * с (tabs)/index, и переход обратно оставлял бы splash в истории.
 *
 * <h2>⚠️ Знак рисуем ТОЛЬКО здесь, системная заставка — пустой фон</h2>
 * Система показывает свою заставку, пока грузится JS, потом кадр держим
 * мы. Раньше в системной был `assets/splash-icon.png`, и на старте знак
 * сначала был маленьким (~126dp: в PNG вокруг знака 30% полей), а потом
 * прыгал до 180 здесь. Заказчик (24.09.2026): «faqat katta logo».
 *
 * Сделать системный знак таким же большим нельзя: Android 12+ обрезает
 * значок заставки кругом диаметром 192dp, а у знака шириной 180 углы
 * выходят за этот круг. Поэтому в `app.json` теперь прозрачный
 * `assets/splash-blank.png` — только фон `#05050A`, тот же, что здесь, и
 * первый знак, который видит человек, — этот, большой.
 *
 * ⚠️ Системная заставка живёт в нативной части: её правка уезжает только
 * с новой сборкой APK, не через `eas update`.
 */

const MARK_WIDTH = 180;

export function SplashOverlay() {
  return (
    <View className="absolute inset-0 items-center justify-center bg-ink">
      <Logo size={MARK_WIDTH} />
    </View>
  );
}
