import { ScreenState } from '@/components/states/ScreenState';

/** TODO: чаты, unread badge, системные сообщения, статусы заявок, deep-link из пуша. */
export default function MessagesScreen() {
  return <ScreenState kind="empty" />;
}
